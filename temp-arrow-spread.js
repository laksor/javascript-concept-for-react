// template string, arrow function, spread operator

const arr = [3, 4, 5, 6, 7];
const student = {
    name: 'sakib',
    age: 45,
    movies: ['king', 'sallu'],
}

// template string back tik : ``
const about = `my name is ${student.name} age of ${student.age} hs number ${number[2]} also like movies ${student.movies[0]}`;
console.log(about);

// arrow function 

